[pytest]
testpaths = backend
asyncio_mode = auto
markers =
    integration: requires a running server, browser, or external network
filterwarnings =
    ignore:Using `httpx` with `starlette.testclient` is deprecated
    ignore:The anyio.abc.BlockingPortal alias is deprecated:DeprecationWarning
