services:
  - type: web
    name: houdini-dark-pattern-prosecutor
    env: docker
    plan: free
    region: oregon
    dockerfilePath: ./Dockerfile
    autoDeploy: true
    envVars:
      - key: PORT
        value: 8000

